{
  "name": "@publisher/schemas",
  "version": "0.1.0",
  "private": true,
  "description": "Publisher schema definitions — JSON Schema source of truth",
  "scripts": {
    "gen": "node codegen/generate.mjs",
    "gen:check": "git diff --exit-code -- '**/*.gen.*'",
    "test": "node codegen/test.mjs",
    "build": "pnpm gen"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
